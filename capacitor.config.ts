import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bangumi.timeline',
  appName: 'bangumi-timeline-web',
  webDir: 'dist'
};

export default config;
